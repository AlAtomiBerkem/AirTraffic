# AirTraffic Project

Приложение для отслеживания расписания рейсов в реальном времени с использованием API Яндекс.Расписаний.

## 🚀 Описание проекта

Данный проект представляет собой веб-приложение, которое отображает актуальное расписание вылетов для конкретной железнодорожной станции или аэропорта. Система состоит из клиентской части на React и прокси-сервера на Node.js/Express для безопасного взаимодействия с внешним API.

## 🛠 Технологический стек

### Client (Фронтенд)
- **React 19**: Современная библиотека для построения интерфейсов.
- **Axios**: Обработка HTTP-запросов.
- **CSS Modules / Vanilla CSS**: Стилизация компонентов.
- **FSD (Feature-Sliced Design)**: Архитектурная методология организации кода (упрощенная версия).

### Server (Бэкенд)
- **Node.js**: Среда выполнения JavaScript.
- **Express**: Легковесный веб-фреймворк.
- **Axios**: Для взаимодействия с Yandex API.
- **CORS**: Настройка кросс-доменных запросов.

## 📂 Структура проекта

```text
 AirTraffic/
 ├── client/               # Клиентская часть (React)
 │   ├── src/
 │   │   ├── app/          # Инициализация приложения, глобальные стили
 │   │   ├── feature/      # Бизнес-фичи (например, FlightSchedule)
 │   │   ├── widgets/      # Композиционные блоки (BackgroundScreen)
 │   │   └── shared/       # Переиспользуемые компоненты и утилиты
 │   └── package.json
 ├── server/               # Бэкенд (Express)
 │   ├── server.js         # Точка входа и логика API
 │   └── package.json
 └── README.md             # Эта документация
```

## ⚙️ Установка и запуск

### 1. Клонирование и подготовка
Убедитесь, что у вас установлен **Node.js**.

### 2. Настройка сервера
```bash
cd server
npm install
node server.js
```
Сервер запустится на `http://localhost:5000`.

### 3. Настройка клиента
```bash
cd client
npm install
npm start
```
Приложение будет доступно по адресу `http://localhost:3000`.

## 🛰 Интеграция с API

Бэкенд выступает посредником для **Yandex Schedule API**.
- **Endpoint**: `/api/flights`
- **Параметры**: `station` (код станции), `date` (дата в формате YYYY-MM-DD).
- **Логика**: Сервер добавляет API-ключ к запросу и возвращает отфильтрованные данные.

### Расчет статусов (Client-side)
Приложение автоматически вычисляет статус рейса на основе данных API:
- **По расписанию**: Рейс ожидается вовремя.
- **Задерживается**: Вычисляется при разнице между плановым и фактическим временем более 15 минут.
- **Отменен**: Если в данных API стоит флаг отмены.

## 🖌 Дизайн

Проект ориентирован на использование в информационных киосках или мобильных устройствах, с фокусом на визуальную чистоту и читаемость расписания. Используется кастомный `BackgroundScreen` для создания атмосферного интерфейса.
